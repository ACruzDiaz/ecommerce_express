{{>layout}}
<main class="register">
  <section id="pagination">
  <div class="productTable">
  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>ID</th>
        <th>Nombre</th>
        <th>Descripción</th>
        <th>Código</th>
        <th>Precio</th>
        <th>Estatus</th>
        <th>Stock</th>
        <th>Categoría</th>
        <th>🧃</th>
      </tr>
    </thead>
    <tbody id="mainTable">
    {{#each allProducts.payload}}
      <tr id="insertTable">
        <th>{{@index}}</th>
        <th class = "pid"> <a href = "http://localhost:8080/products/{{this._id}}">{{this._id}}</a></th>
        <th>{{this.title}}</th>
        <th>{{this.description}}</th>
        <th>{{this.code}}</th>
        <th>{{this.price}}</th>
        <th>{{this.status}}</th>
        <th>{{this.stock}}</th>
        <th>{{this.category}}</th>
        <th><button class = "add2Cart"> Agregar al carrito </button></th>
      </tr>
    {{/each}}
    </tbody>
  </table>
  </div>
  {{#if allProducts.hasPrevPage}}
    <button type="button" id="prevPage">Anterior</button>
  {{/if}}
  {{#if allProducts.hasNextPage}}
    <button type="button" id="nextPage">Siguiente</button>
  {{/if}}
  </section>
  <form id="productsForm">
    <section id="addProduct">
      <label for="title">Title</label>
      <input type="text" name="title" id="title"/>
      <label for="description">Description</label>
      <input type="text" name="description" id="description"/>
      <label for="code">Code</label>
      <input type="text" name="code" id="code"/>
      <label for="price">Price</label>
      <input type="number" name="price" id="price"/>
      <label for="status">Status</label>
      <input type="checkbox" name="status" id="status" value="selected" checked/>
      <label for="stock">Stock</label>
      <input type="number" name="stock" id="stock"/>
      <label for="category">Category</label>
      <input type="text" name="category" id="category"/>
      <button id="create">Create</button>
    </section>
    <hr/>
    <section id="deleteProduct">
      <label for="pid">Product ID</label>
      <input type="text" name="pid" id="pid"/>
      <button id="delete">Delete</button>
    </section>
    <hr/>
    <section id="cartSection">
      <button id="thisCart">Ver mi Carrito</button>
    </section>
  </form>
  
</main>